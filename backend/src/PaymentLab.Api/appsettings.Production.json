{
  "_README": "RED ZONE FILE. Production settings are owned by Platform + Compliance and are changed only through the change-management ticket, never by an agent and never inside a feature branch. Values below are placeholders for the lab; real values come from the secret vault at deploy time.",

  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Error"
    }
  },
  "ConnectionStrings": {
    "Ledger": "INJECTED_BY_VAULT_AT_DEPLOY",
    "AuditStore": "INJECTED_BY_VAULT_AT_DEPLOY"
  },
  "Payments": {
    "RequireIdempotencyKey": true,
    "MaxRefundPerRequestIdr": 25000000,
    "DualApprovalThresholdIdr": 10000000,
    "SettlementCutOffLocalTime": "21:00"
  },
  "Compliance": {
    "MaskCardNumbers": true,
    "AuditRetentionDays": 3650,
    "OnshoreProcessingOnly": true,
    "IncidentReportingEnabled": true
  }
}
