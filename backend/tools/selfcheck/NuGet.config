<?xml version="1.0" encoding="utf-8"?>
<!--
  This harness deliberately has zero package dependencies, so we clear every
  package source. That makes it runnable on a locked down laptop where
  nuget.org is blocked, which is the whole point of it.
-->
<configuration>
  <packageSources>
    <clear />
  </packageSources>
</configuration>
